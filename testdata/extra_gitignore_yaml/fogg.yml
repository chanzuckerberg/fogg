defaults:
  backend:
    bucket: bucket
    profile: foo
    region: region
  owner: foo@example.com
  project: foo
  providers: {}
  terraform_version: 1.1.1
  extra_gitignore:
    - "*.log"
    - "secrets/"
    - ".env.local"
version: 2

# Auto-generated by fogg. Do not edit
# Make improvements in fogg, so that everyone can benefit.

module "{{.ModuleName}}" {
  source = "{{.ModuleSource}}"
  {{range $i, $kv := .Variables -}}
    {{index $kv 0}} = {{index $kv 1}}
  {{ end}}

  {{ if .ProviderAliases -}}
  providers = {
    {{ range $k, $v := .ProviderAliases -}}
    {{$k}} = {{$v}}
    {{end -}}
  }{{ end }}
}

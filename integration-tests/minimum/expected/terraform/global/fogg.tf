# Auto-generated by fogg. Do not edit
# Make improvements in fogg, so that everyone can benefit.

provider "aws" {
  version = "~> 100.0.0"
  region  = "region_provider"
  profile = "profile_provider"
}

# Aliased Providers (for doing things in every region).

terraform {
  required_version = "~>0.1000.0"

  backend "s3" {
    bucket = "bucket"

    key = "terraform/project/global.tfstate"

    encrypt = true
    region  = "region_backend"
    profile = "profile_backend"
  }
}

variable "env" {
  type    = "string"
  default = ""
}

variable "project" {
  type    = "string"
  default = "project"
}

variable "region" {
  type    = "string"
  default = "region_provider"
}

variable "component" {
  type    = "string"
  default = "global"
}

variable "aws_profile" {
  type    = "string"
  default = "profile_provider"
}

variable "owner" {
  type    = "string"
  default = "me@example.com"
}

variable "tags" {
  type = "map"

  default = {
    project   = "project"
    env       = ""
    service   = "global"
    owner     = "me@example.com"
    managedBy = "terraform"
  }
}

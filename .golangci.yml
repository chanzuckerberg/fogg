run:
  skip-dirs:
    - exp/examine
    # - cmd/exp
  skip-files:
    - cmd/exp/entropy.go
    - exp/examine/local.go
    - exp/examine/util.go
  linters:
    enable:
      - gofmt
      - whitespace
      - unparam
      - scopelint
